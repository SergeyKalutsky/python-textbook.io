# Len

Встроенный метод len возвращает кол-во элементов в объекте
```py
>>> lst = ["A", "B", "C"]
>>> print(len(lst))
3
>>> string = 'hello'
>>> print(len(string))
5
>>> d = {'key': 2, 'key2': 3}
>>> print(len(d))
2
```